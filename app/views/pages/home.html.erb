
      <div class="jumbotron text-center">
        <% if user_signed_in? %>
        <h1>Hi <%= current_user.name %></h1>
        <p>Welcome to Student Evaluations</p>
        <% else %>
        <h1>Welcome to Student Evaluations</h1>
        <% end %>
      </div>

      <div class="row marketing">

        <div class="col-md-4 col-md-offset-4 text-center">
        <% if !user_signed_in? %>
          <p class="lead">Want to evaluate your students?</p>
          <p> <%= link_to "Sign in", new_user_session_path, class: "btn btn-lg btn-success" %></p>
        <% else %>
          <p class="lead">Go to Classes</p>
          <p> <%= link_to "Classes", batches_path, class: "btn btn-lg btn-success" %><p>
        <% end %>
        </div>

      </div>
